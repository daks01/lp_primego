---
const {
  as: Element = "button",
  href,
  class: className,
  ...props
} = Astro.props;
---

<Element {...props} href={href} class:list={["button", className]}>
  <slot />
</Element>

<style>
.button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    margin: 0;
    transition: filter .1s;
    border: none;
	border-radius: 100px;
    padding: calc(var(--adaptive-pixel) * 14) calc(var(--adaptive-pixel) * 20);
    background: var(--color-product);
    color: var(--button-text-color, --color-white);
    font-weight: 700;
	font-size: calc(var(--adaptive-pixel) * 20);
    font-family: inherit;
    text-decoration: none;
    white-space: nowrap;
    cursor: pointer;
    user-select: none;

    &:hover {
        filter: brightness(105%);
    }
    &:active {
        filter: brightness(97%);
    }
}

.button_ticket {
    border-radius: 4px;
    clip-path: polygon(
        0 0, 
        47% 0, 
        50% 17%, 
        53% 0, 
        100% 0, 
        100% 100%, 
        53% 100%, 
        50% 83%, 
        47% 100%, 
        0 100%
    );
    padding: calc(var(--adaptive-pixel) * 20) calc(var(--adaptive-pixel) * 32);
}


@media screen and (width < 768px) {
    .button {
        width: 100%;
    }
}
</style>
